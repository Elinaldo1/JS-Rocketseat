//VALORES PADRÃO=================================
  // colocar valores padrão para os parâmetros
  const soma =(a=1,b=1)=>a+b;
  console.log(soma());//vai retornar NaN
//FIM VALORES PADRÃO=================================